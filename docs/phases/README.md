# Phase Plan

This project follows a compact spec-driven workflow:

1. Read `docs/specs/` to confirm scope and behavior.
2. Implement one phase at a time.
3. Validate output before marking complete.
4. Update `STATUS.md` and commit.
